dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "jikan-client-compose"
include ':app'
enableFeaturePreview("VERSION_CATALOGS")
